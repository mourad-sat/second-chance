import { AuthForm } from "@/components/AuthForm";

export default function SetupPage() {
  return <main className="grid min-h-screen place-items-center bg-slate-100 p-4"><AuthForm mode="setup" /></main>;
}
